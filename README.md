Download all pages of `getFeeds` and `getFeedVersions` endpoints:
```
pipenv run python scrape-transitfeeds.py
```

Merge together JSON files for each page:
```
pipenv run python merge-json-files.py
```
